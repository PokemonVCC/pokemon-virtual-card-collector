const nanosecondsPerSecond = 1e9;
const nanosecondsToMs = 1e6;

const endingTypes = {
    FINISHED: 'FINISHED',
    CLOSED: 'CLOSED'
};

module.exports = {
    endingTypes,
    nanosecondsPerSecond,
    nanosecondsToMs
};